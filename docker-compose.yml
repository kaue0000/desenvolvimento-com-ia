services:
  ollama:
    image: ollama/ollama
    container_name: ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama-data:/root/.ollama

  frontend:
    build:
      context: ./encontro08/frontend
    user: "${LOCAL_UID:-1000}:${LOCAL_GID:-1000}"
    environment:
      HOME: /tmp
    ports:
      - "4200:4200"
    volumes:
      - ./encontro08/frontend:/app
      - frontend_node_modules:/app/node_modules

volumes:
  ollama-data:
    name: encontro05_ollama-data
  frontend_node_modules: